<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Đường viền -->
    <stroke
        android:width="2dp"
        android:color="#D17842" />

    <!-- Màu nền -->
    <solid android:color="#ffffff" />

    <!-- Góc bo tròn của đường viền -->
    <corners android:radius="15dp" />
</shape>